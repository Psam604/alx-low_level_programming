#include "main.h"

/**
 * _abs - print the absolute value of numbers
 * @n: number to be evaluated
 *
 * Return: absolute value of n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
